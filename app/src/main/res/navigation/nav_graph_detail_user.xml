<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph_detail_user"
    app:startDestination="@id/detailUserFragment">

    <fragment
        android:id="@+id/detailUserFragment"
        android:name="com.android.tymexmobile.feature.detail_user.DetailUserFragment"
        tools:label="DetailUserFragment"
        tools:layout="@layout/fragment_user_detail">
        <argument
            android:name="user"
            android:defaultValue="@null"
            app:argType="com.android.model.User"
            app:nullable="true" />
    </fragment>
</navigation>